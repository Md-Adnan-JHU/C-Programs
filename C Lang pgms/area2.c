#include<stdio.h>
#include<math.h>
int main()
{  int l,b,area;
    printf("enter the length and breadth:");
    scanf("%d %d",&l,&b);
    area=l*b;
    printf("the area of rectangle of length %d and breadth %d is %d",l,b,area);
    return 0;
    }
